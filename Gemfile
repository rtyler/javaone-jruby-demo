source 'https://rubygems.org'

gem 'pry'
# For prettifying pry
gem 'pry-theme'

# We need docs in pry!~
gem 'rdoc-data'

# colorize is used for running ascii table with pretties
gem 'colorize'

gem 'concurrent-ruby'
